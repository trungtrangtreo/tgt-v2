<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:autofit="http://schemas.android.com/apk/res-auto"
    android:id="@+id/list_item_achievement"
    android:layout_width="match_parent"
    android:layout_height="@dimen/list_item_achievement_height"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/list_item_achievement_iv"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:scaleType="fitXY" />

    <me.grantland.widget.AutofitTextView
        android:id="@+id/list_item_achievement_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:maxLines="2"
        android:padding="@dimen/list_item_achievement_title_padding_top"
        android:textColor="@color/colorPrimary"
        android:textSize="15sp"
        autofit:minTextSize="12sp" />

</LinearLayout>